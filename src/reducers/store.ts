import { IStore } from "models/store";

export default (state: IStore = { items: [] }, action: {}) => {
    // switch (action.type) {
    //     case getType(auth.login)        default:
    //         return state;
    // }

    return state;
};
